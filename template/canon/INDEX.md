# Canon Index

This file is the navigation surface for everything in `characters/`, `companies/`, `places/`, and `factions/`. One short paragraph per entity, grouped by type. Read this first on every run; pull the full files via `read_file` only when an entity is relevant to the current snapshot or task.

The paragraphs below mirror each entity file's `quick_read:` frontmatter field. When you create or substantially update an entity, update both the full file and this index — they're the same prose in two places.

## Characters

_None yet — entries appear here as characters are introduced._

## Companies

_None yet — entries appear here as companies are introduced._

## Places

_None yet — entries appear here as neighborhoods, landmarks, and districts are introduced._

## Factions

_None yet — entries appear here as factions are introduced._

---

**Format per entry.** Use a `### Name` heading, then the one-paragraph `quick_read` body. Keep entries short — if you need more than ~3 sentences, the detail belongs in the full file, not here. Reference the full file path at the end of each entry so the reader knows where to dig:

```
### Maria Chen
Founder of MeshNet Pizza in Westside; widow; quietly funds the Friendship Park library. Active in pro-density politics since the 2024 rezoning fight. See: characters/maria-chen.md.
```
